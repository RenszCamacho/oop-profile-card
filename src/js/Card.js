class Card {
  constructor(imageProfile, fullName, age, city, followers, likes, photos) {
    this.imageProfile = imageProfile;
    this.fullName = fullName;
    this.age = age;
    this.city = city;
    this.followers = followers;
    this.likes = likes;
    this.photos = photos;
  }
}

export default Card;
